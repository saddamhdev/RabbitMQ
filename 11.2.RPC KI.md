**RPC এর Full Meaning হলো → Remote Procedure Call**

---

# 🟦 **RPC (Remote Procedure Call) কী? — সহজ ভাষায়**

👉 RPC মানে হলো **এক সার্ভার থেকে আরেক সার্ভারের ফাংশন বা মেথডকে এমনভাবে কল করা**,
যেন সেটা **লোকাল ফাংশন**।

মানে তুমি কোডে শুধু function call করবে—

```
result = doPayment(userId);
```

কিন্তু আসলে function টি অন্য server / অন্য microservice এ চলবে।

---

# 🟩 RPC ব্যবহার করলে কী সুবিধা?

✔ API-এর মতো কাজ করে
✔ Producer শুধুমাত্র request পাঠায়
✔ Consumer logic execute করে
✔ Producer direct response পায়

এটাই RabbitMQ-তে Direct Reply-To করে।

---

# 🟪 Where RPC is Used?

* Microservices communication
* Payment validation
* User authentication
* Real-time scoring
* Chatbot response
* Quiz submission → Live Result

---

# ⭐ One-line Summary

👉 **RPC = Remote Procedure Call → দূরের সার্ভারের ফাংশনকে নিজের কোডের মতো কল করা।**

---

আরও চাইলে:

✔ RPC vs REST API difference
✔ RabbitMQ RPC vs HTTP RPC
✔ Why Direct Reply-To is best for RPC

বললেই বলে দিচ্ছি!
